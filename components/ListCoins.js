import React from "react";
import { View, Text } from "react-native";

function ListCoins({ navigation }) {
    return <View>
        <Text> List Coins </Text>
    </View>
}

export default ListCoins;